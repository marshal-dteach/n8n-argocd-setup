apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n-deployment
  namespace: n8n
  labels:
    app: n8n
spec:
  replicas: 1
  selector:
    matchLabels:
      app: n8n
  template:
    metadata:
      labels:
        app: n8n
    spec:
      volumes:
        - name: n8n-pv-storage
          PersistentVolumeClaim:
            claimName: n8n-pv-claim
      containers:
      - name: n8n
        image: n8nio/n8n:1.114.2
        ports:
        - containerPort: 5678
        volumeMounts:
          - mountPath: "/home/node/.n8n"
            name: n8n-pv-storage

